Finish and verify the Dirty 30 live beta in the existing deuell1/Dirty30 repository.

Do not redesign the interface, regenerate the project, add new product features, create another mockup, or replace the existing React/Vite, Express, Clerk, Drizzle, OpenAPI, PostgreSQL, and Replit architecture.

The current implementation is partially functional but does not yet satisfy the locked beta requirements. Correct the verified gaps below.

1. Restore the correct roster requirement

The roster limit is exactly 8 occupied positions per team, not 15.

An occupied position is:

* One active team membership, or
* One non-expired pending invitation

Correct every occurrence of 15 in code, documentation, dashboard calculations, messages, seed data, and tests.

Implement a reusable roster-capacity service. All roster-changing operations must call it.

Enforce the limit inside a PostgreSQL transaction with team-level locking or another concurrency-safe PostgreSQL mechanism. Two simultaneous invitations or membership changes must never create more than 8 occupied positions.

Expired invitations must be changed to EXPIRED or excluded consistently before capacity is calculated.

2. Complete roster workflows

Add persistent, authorized API endpoints and functional UI controls for:

* Assign or replace team captain
* Invite player
* Display secure copyable invitation link
* Regenerate/resend an invitation link
* Cancel pending invitation
* Accept invitation
* Remove active player
* Update the authenticated player’s profile
* Activate or deactivate a team

Rules:

* Commissioner can manage any team.
* Captain can manage only their assigned team.
* Players cannot manage rosters.
* Commissioner cannot bypass the eight-position limit.
* Only the invited normalized email can accept an invitation.
* Invitation tokens expire, are single-use, and are stored only as hashes.
* Acceptance and membership creation must be atomic.
* Duplicate pending invitations must be rejected.
* Removing a captain must not silently leave the user with incorrect captain permissions.
* No jersey numbers, temporary players, or placeholder players.

Update the OpenAPI specification first, regenerate the client and Zod packages, then connect every UI control to the generated API hooks.

3. Complete schedule administration

Add commissioner-only endpoints and mobile UI controls for:

* List, create, edit and deactivate venues
* List, create, edit and deactivate courts
* Create game
* Edit draft or published game
* Publish game
* Cancel game

Reject:

* Same home and away team
* Inactive team
* Inactive venue or court
* Game outside the active season
* Same team scheduled in overlapping games
* Same court scheduled in overlapping games
* Invalid or missing date/time values

Conflict checks must be enforced server-side and concurrency-safe.

Draft games must remain commissioner-only. Other authenticated league users may see published, pending-confirmation, disputed, final and cancelled games.

Do not implement automatic schedule generation yet.

4. Complete score confirmation and disputes

The required state transitions are:

PUBLISHED → PENDING_CONFIRMATION
PENDING_CONFIRMATION → FINAL
PENDING_CONFIRMATION → DISPUTED
DISPUTED → FINAL through commissioner resolution
FINAL → FINAL through audited commissioner correction

Implement:

* Opposing captain confirmation
* Opposing captain dispute with required reason
* Commissioner dispute resolution
* Commissioner score correction
* Commissioner direct score entry when necessary
* Complete audit history for every score transition

The captain who submitted the score cannot confirm it.

A captain from the submitting team cannot confirm it merely because they are a different account. Confirmation or dispute must come from a captain assigned to the opposing team.

Final scores are locked from captain editing.

Commissioner corrections must record previous and new values in AuditEvent.

Expose appropriate confirm/dispute controls on the game-detail screen for the opposing captain. The commissioner review queue must display the dispute reason and provide real resolve/correct actions.

Remove the obsolete client-only Review implementation and retain only the API-backed review workflow.

5. Correct standings

Calculate standings from FINAL games only.

Include:

* Rank
* Games played
* Wins
* Losses
* Points for
* Points against
* Point differential

Sort by:

1. Wins
2. Point differential
3. Points for
4. Team name

Ensure draft, published, pending, disputed and cancelled games have no effect.

6. Complete role-aware UI

Replace any remaining hardcoded user names, roles, counts or messages with authenticated API data.

Add a current-user endpoint and use it throughout the interface.

Required behavior:

* Commissioner sees commissioner actions.
* Captain sees only actions allowed for their team.
* Player sees read-only league information and personal profile controls.
* Unauthorized buttons are absent.
* Server authorization remains authoritative.
* API permission and validation errors appear clearly in the interface.
* All workflows remain usable from a phone.

Do not treat hidden buttons as authorization.

7. Correct seed data

Make the development seed idempotent and include:

* One commissioner
* Four captains
* Four teams
* Several active players
* One valid pending invitation
* One team with exactly 8 occupied positions
* Venue and courts
* Draft game
* Published game
* Pending-confirmation game
* Disputed game
* Final games

Do not automatically seed production.

Document how real Clerk users become linked to seeded records without enabling account impersonation or privilege escalation.

8. Add real automated tests

Vitest and Supertest are installed, but there are no committed tests or test scripts. Add test scripts and a test database strategy.

Test at minimum:

Authentication and authorization

* Unauthenticated league request returns 401.
* Player roster mutation returns 403.
* Captain cannot manage another team.
* Captain cannot submit an unrelated game.
* User cannot promote themselves.
* Submitting team cannot confirm its own score.
* Opposing captain can confirm or dispute.

Rosters

* Active members and pending invitations both count toward 8.
* Ninth position is rejected.
* Simultaneous requests cannot exceed 8.
* Duplicate pending invitation is rejected.
* Canceling an invitation opens a position.
* Removing a player opens a position.
* Expired invitation opens a position.
* Wrong email cannot accept.
* Token cannot be reused.

Schedule

* Team conflict is rejected.
* Court conflict is rejected.
* Invalid season date is rejected.
* Draft visibility is restricted.
* Published game is visible.

Scores

* Submission enters pending confirmation.
* Confirmation finalizes.
* Dispute records a reason.
* Commissioner resolution finalizes.
* Captain cannot edit final score.
* Commissioner correction creates an audit record.
* Only final games affect standings.

9. Restore quality controls

* Remove noImplicitReturns: false and correct the underlying TypeScript return errors.
* Add lint scripts and a reasonable ESLint configuration.
* Add test scripts at the root and relevant workspaces.
* Remove unused prototype functions and dead mockup code that is not required by the deployed application.
* Do not delete reusable UI components merely because they are currently unused.
* Confirm no hardcoded application data remains in production paths.
* Confirm no secrets are committed.

10. Production verification

Before reporting completion:

1. Generate and review migrations.
2. Apply migrations to the Replit development database.
3. Run the seed twice to prove idempotency.
4. Regenerate OpenAPI clients and validators.
5. Run formatting.
6. Run linting.
7. Run strict type checking.
8. Run every automated test.
9. Run the production build.
10. Start the production configuration.
11. Call /api/healthz.
12. Sign in as commissioner, captain and player.
13. Complete one end-to-end roster invitation.
14. Complete one schedule creation and publication.
15. Complete one score submission and opposing-captain confirmation.
16. Complete one score dispute and commissioner resolution.
17. Restart the server and verify the records remain.
18. Verify standings reflect only final scores.
19. Verify the deployed app works on a mobile viewport.

Do not claim a check passed unless it was actually executed. Report blockers plainly.

Update the README to state the correct eight-position roster limit.

Commit and push the completed work to deuell1/Dirty30 on main with:

Complete and verify Dirty 30 live beta workflows

Report:

* Files changed
* API endpoints completed
* Migration applied
* Seed results
* Tests added and passing count
* Lint and strict type-check results
* Production build result
* Authentication results by role
* End-to-end workflow results
* Restart persistence result
* Deployment URL
* Commit SHA
* Any remaining blocker

Stop when the defined live-beta workflows are complete. Do not start chat, waivers, payments, substitute pool, temporary players, availability, automated scheduling, or native-app packaging.